development:
  secret_key_base: development_secret_key_base_change_in_production

test:
  secret_key_base: test_secret_key_base_for_specs_only

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
